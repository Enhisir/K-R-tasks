#include <stdio.h>

#define BIT_LIMIT 32

int rightrot(unsigned int, unsigned int);

int main() {
    // printf("%u\n", );
    

    return 0;
}

int rightrot(unsigned int x, unsigned int y) {
    int i;
    unsigned int rest;
    y = y % BIT_LIMIT;
    rest = x 
}